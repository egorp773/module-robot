# TEMPLATE ONLY. Do not enable until IMU yaw is proven to be REP-103 ENU and the
# TODO_CALIBRATE values below are replaced from a recorded heading-init result.
navsat_transform:
  ros__parameters:
    frequency: 10.0
    delay: 0.0
    transform_timeout: 0.20
    magnetic_declination_radians: 0.0  # TODO_CALIBRATE
    yaw_offset: 0.0                    # TODO_CALIBRATE
    zero_altitude: false
    publish_filtered_gps: true
    broadcast_utm_transform: false
    broadcast_utm_transform_as_parent_frame: false
    use_odometry_yaw: false
    wait_for_datum: false
